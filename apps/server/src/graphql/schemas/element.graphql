interface Element {
  id: ID!
  x: Float!
  y: Float!
  width: Float!
  height: Float!
  angle: Float!
  scaleX: Float!
  scaleY: Float!
  slide: Slide!
  # index in an array of elements
  position: Int!
}
