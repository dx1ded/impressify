type Shape implements Element {
  id: Int!
  layer: Int!
  x1: Int!
  y1: Int!
  x2: Int!
  y2: Int!
  angle: Int!
  slide: Slide!

  fillColor: String!
  strokeColor: String!
  strokeWidth: Int!
  # "1/1", "1/2", etc, OR null (meaning no aspectRatio)
  aspectRatio: String
  points: [[Int!]!]!
}
