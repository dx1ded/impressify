type Text implements Element {
  id: Int!
  x: Int!
  y: Int!
  width: Int!
  height: Int!
  angle: Int!
  slide: Slide!

  text: String!
  fillColor: String!
  borderColor: String!
  textColor: String!
  fontFamily: String!
  fontSize: Int!
  bold: Boolean!
  italic: Boolean!
  underlined: Boolean!
  alignment: String!
  lineHeight: Int!
}
