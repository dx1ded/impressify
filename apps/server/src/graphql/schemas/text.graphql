type Text implements Element {
  id: Int!
  layer: Int!
  x1: Int!
  y1: Int!
  x2: Int!
  y2: Int!
  angle: Int!
  slide: Slide!

  text: String!
  fillColor: String!
  borderColor: String!
  textColor: String!
  fontFamily: String!
  fontSize: Int!
  bold: Boolean!
  italic: Boolean!
  underlined: Boolean!
  lineHeight: Int!
}
