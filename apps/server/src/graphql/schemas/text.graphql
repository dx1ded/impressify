type Text implements Element {
  id: Int!
  x: Int!
  y: Int!
  width: Int!
  height: Int!
  angle: Int!
  scaleX: Int!
  scaleY: Int!
  slide: Slide!

  text: String!
  textColor: String!
  fillColor: String!
  borderColor: String!
  fontFamily: String!
  fontSize: Int!
  bold: Boolean!
  italic: Boolean!
  underlined: Boolean!
  alignment: String!
  lineHeight: Int!
}
